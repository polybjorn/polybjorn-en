---
import Layout from '../layouts/Layout.astro';
import { t } from '../data/translations.js';
import { Link2Off } from 'lucide-astro';
---

<Layout title="404 - Page not found" description="This page doesn't exist." noindex>
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    body {
      background: #1e2228;
      color: #dde1e7;
      font-family: 'Piazzolla', serif;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      text-align: center;
    }

    .container {
      padding: 2rem;
    }

    .icon {
      color: #6aaeee;
      opacity: 0.7;
      margin-bottom: 1rem;
    }

    h1 {
      font-family: 'Piazzolla', serif;
      font-size: 4rem;
      color: #fff;
      margin-bottom: 0.5rem;
    }

    p {
      font-family: 'Piazzolla', serif;
      font-size: 1.2rem;
      color: #ccc;
      margin-bottom: 2rem;
    }

    a {
      font-family: 'Piazzolla', serif;
      color: #6aaeee;
      text-decoration: underline;
      font-size: 1.1rem;
    }

    a:hover {
      color: #fff;
    }
  </style>

  <main id="main">
    <div class="container">
      <Link2Off size={64} strokeWidth={1.25} class="icon" />
      <h1>404</h1>
      <p id="not-found-msg"></p>
      <a id="not-found-link" href="/"></a>
    </div>
  </main>

  <script define:vars={{ strings: { en: { msg: t.en.notFound, home: t.en.goHome }, no: { msg: t.no.notFound, home: t.no.goHome } } }}>
    const isNo = window.location.hostname === 'polybjorn.no';
    const tr = isNo ? strings.no : strings.en;
    document.getElementById('not-found-msg').textContent = tr.msg;
    const link = document.getElementById('not-found-link');
    link.textContent = tr.home;
    link.href = '/';
  </script>
</Layout>
